language: rust
rust:
  - nightly
cache: cargo
before_install:
before_script:
  - rustup component add rustfmt-preview
script:
  - cargo fmt -- --version
  - cargo fmt -- --write-mode=diff
  - cargo build
  - cargo test
notifications:
  email: false
